{
  "installCommand": "cd .. && npm ci && cd frontend && npm ci",
  "builds": [
    { "src": "package.json", "use": "@vercel/next" },
    { "src": "api/index.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/index.js" }
  ]
}
